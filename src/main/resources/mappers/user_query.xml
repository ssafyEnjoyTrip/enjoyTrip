<?xml version="1.0" encoding="UTF-8"?>
	
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.enjoyTrip.dao.EmpDao">
	<select id="empDetail" parameterType="int" resultType="com.example.enjoyTrip.dto.UserDto">
		select user_Id, user_password, user_email, user_name, user_age
		from user where user_id = #{userId}
	</select>
	
	<!-- return이 Collections 이면 Dto만 기술 -> 리턴타입에 List 안와도 됨 -->
	<select id="empList" resultType="com.example.enjoyTrip.dto.UserDto">
		select user_Id, user_password, user_email, user_name, user_age
		from user
	</select>
	
	<!-- return이 primitive 타입 이면 별도로 기술할 필요가 없다. (int) -->
	<insert id="empInsert" parameterType="com.example.enjoyTrip.dto.UserDto">
		insert into user (user_Id, user_password, user_email, user_name, user_age)
		values(#{userId},#{userPassword},#{email},#{userName},#{userAge})
	</insert>
	
	<update id="empUpdate" parameterType="com.example.enjoyTrip.dto.UserDto">
		update user set user_password=#{userPassword}, user_email=#{email}, user_name=#{userName}, user_age=#{userAge}
		where user_id = #{userId}
	</update>
	
	<delete id="empDelete" parameterType="int">
		delete from user where user_id = #{userId}
	</delete>
	
	
</mapper>